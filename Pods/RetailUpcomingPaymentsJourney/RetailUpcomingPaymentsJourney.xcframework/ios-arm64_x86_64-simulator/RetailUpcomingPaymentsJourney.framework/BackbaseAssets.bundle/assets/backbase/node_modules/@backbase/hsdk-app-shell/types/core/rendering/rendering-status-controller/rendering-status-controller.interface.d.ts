import { ViewInstanceId } from '../../views/view-instance/view-instance.interface';
export interface ViewRenderingStatusRecord {
    id: ViewInstanceId;
    rendered: boolean;
}
export interface RenderingStatusController {
    setViewInstanceReady: (id: string) => void;
    whenViewInstanceReady: (id: string) => Promise<void>;
}
