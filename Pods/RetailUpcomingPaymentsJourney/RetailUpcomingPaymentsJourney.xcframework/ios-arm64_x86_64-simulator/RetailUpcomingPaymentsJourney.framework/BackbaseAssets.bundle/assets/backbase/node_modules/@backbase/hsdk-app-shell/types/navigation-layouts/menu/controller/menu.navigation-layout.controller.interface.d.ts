import { NavigationLayoutController, NavigationResult, StackNavigationLayoutController, Transition } from '../../../types';
export interface MenuNavigationLayoutTransitions {
    select: Transition;
}
export interface MenuNavigationLayoutController extends NavigationLayoutController {
    activeMenuItem: string;
    activeStack: StackNavigationLayoutController;
    id: string;
    menuItems: Array<string>;
    transitions: MenuNavigationLayoutTransitions | null;
    select: (targetURI: string) => Promise<NavigationResult>;
}
