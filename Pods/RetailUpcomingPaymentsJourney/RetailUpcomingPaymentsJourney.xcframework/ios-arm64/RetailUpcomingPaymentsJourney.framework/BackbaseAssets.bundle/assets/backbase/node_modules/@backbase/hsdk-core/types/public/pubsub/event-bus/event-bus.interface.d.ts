import { JSONMap } from '../../../utils/json.interface';
export interface EventPayload extends JSONMap {
}
export declare type EventListenerFn = (eventPayload: EventPayload) => void;
export declare type EventName = string;
export declare type Publish = (eventName: EventName, payload: EventPayload) => void;
export declare type Subscribe = (eventName: EventName, eventListener: EventListenerFn) => void;
export declare type Unsubscribe = (eventName: EventName, eventListener: EventListenerFn) => void;
export interface EventBus {
    name: string;
    publish: Publish;
    subscribe: Subscribe;
    unsubscribe: Unsubscribe;
}
