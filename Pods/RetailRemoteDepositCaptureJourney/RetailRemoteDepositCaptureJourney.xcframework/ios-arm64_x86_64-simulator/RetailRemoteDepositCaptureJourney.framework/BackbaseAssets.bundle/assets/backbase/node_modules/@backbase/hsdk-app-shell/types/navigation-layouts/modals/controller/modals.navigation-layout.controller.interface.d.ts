import { NavigationResult } from '../../../core/navigation/navigation-pipeline/navigation-pipeline.interface';
import { NavigationLayoutController, Transition } from '../../../types';
export interface ModalsNavigationLayoutTransitions {
    dismiss: Transition;
    open: Transition;
}
export interface ModalsNavigationLayoutController extends NavigationLayoutController {
    dismiss: (stackId: string) => Promise<NavigationResult>;
    activeStack: string | null;
    size: number;
    open: (origin: string, targetURI: string) => Promise<NavigationResult>;
    transitions: ModalsNavigationLayoutTransitions | null;
}
