export declare type LifecycleCallback = () => void;
export interface Lifecycle {
    canNavigate: (navigationHook: NavigationHook) => void;
    getNavigationHooks: () => Array<NavigationHook>;
    navigationDidFinish: (callback: LifecycleCallback) => void;
    navigationWillStart: (callback: LifecycleCallback) => void;
    renderTreeDidChange: (callback: LifecycleCallback) => void;
}
export interface NavigationHookContext {
    originURI: string;
    targetURI: string | null;
}
export declare type NavigationHook = (navigationHookContext: NavigationHookContext) => Promise<boolean>;
