import { EventPayload } from '../../../libs/event-emitter/event-emitter.interface';
import { ViewInstanceId } from '../../views/view-instance/view-instance.interface';
import { NavigationResult } from '../navigation-pipeline/navigation-pipeline.interface';
export interface NavigationEventPayload extends EventPayload {
    origin: ViewInstanceId;
    payload: EventPayload;
}
export interface NavigationController {
    navigate: (origin: string, targetURI: string) => Promise<NavigationResult>;
    startNavigation: () => Promise<void>;
}
