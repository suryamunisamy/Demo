import { NavigationResult } from '../../../core/navigation/navigation-pipeline/navigation-pipeline.interface';
import { NavigationLayoutController, Transition } from '../../../types';
import { StackNavigationLayoutController } from '../../stack/controller/stack.navigation-layout.controller.interface';
export interface TabsNavigationLayoutTransitions {
    select: Transition;
}
export interface TabsNavigationLayoutController extends NavigationLayoutController {
    activeTab: number;
    activeStack: StackNavigationLayoutController;
    tabs: Array<string>;
    select: (viewId: string) => Promise<NavigationResult>;
    transitions: TabsNavigationLayoutTransitions | null;
}
