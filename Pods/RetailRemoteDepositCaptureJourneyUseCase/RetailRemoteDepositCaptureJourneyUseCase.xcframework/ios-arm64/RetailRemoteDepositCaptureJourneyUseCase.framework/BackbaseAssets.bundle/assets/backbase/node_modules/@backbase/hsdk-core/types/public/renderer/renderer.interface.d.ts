import { Config } from '../config/config.interface';
import { MBAASPortalModel, MBAASPortalModelPage } from '../portal-model/portal-model.interface';
export declare type CreateRenderer = (config: Config) => Renderer;
export interface Renderer {
    name: string;
    renderItem: (itemModel: MBAASPortalModel | MBAASPortalModelPage) => Promise<void>;
}
