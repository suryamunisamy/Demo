export declare type EventListenerFn = (eventPayload: EventPayload) => void;
export interface EventPayload {
}
export interface EventEmitter {
    emit: (eventName: string, payload: EventPayload) => void;
    off: (eventName: string, eventListener: EventListenerFn) => void;
    on: (eventName: string, eventListener: EventListenerFn) => void;
    name: string;
}
